import NotFound from "@/components/not-found/NotFound";

export default function NotFoundPage() {
  return (
    <div className="h-full flex items-center justify-center xl:pt-[5vh] xl:pb-[5vh] max-lg:pb-[20px]">
      <NotFound />
    </div>
  );
}
